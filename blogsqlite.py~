import sqlite3

conn = sqlite3.connect("blog.db")

p = "create table posts(post_id integer, title text, content text)"

c = "create table comments(comment_id integer, content text, p_id integer)"

curs = conn.cursor()
curs.execute(p)
curs.execute(c)
conn.commit();
